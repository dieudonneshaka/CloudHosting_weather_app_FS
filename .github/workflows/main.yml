name: CloudHosting

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  CloudHosting:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. Setup .NET SDK
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '9.0'

      # 3. Restore dependencies
      - name: Restore
        run: dotnet restore ./currentIimeservice.sln   # Removed --configuration

      # 4. Build the solution
      - name: Build
        run: dotnet build ./currentIimeservice.sln --configuration Release

      # 5. Publish the project
      - name: Publish
        run: dotnet publish ./currentIimeservice.sln -c Release -o publish_output
