name: CloudHosting

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  CloudHosting:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. Setup .NET SDK
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '9.0'

      # 3. Restore dependencies
      - name: Restore
        run: dotnet restore ./currentIimeservice.sln --configuration Release

      # 4. Build the solution
      - name: Build
        run: dotnet build ./currentIimeservice.sln --configuration Release

      # 5. (Optional) Run your project or publish
      - name: Run CloudHosting
        run: dotnet run --project ./currentIimeservice.sln --configuration Release
